{
  "name": "react-forms-validation",
  "version": "0.0.1-beta1",
  "authors": "Thomas Winckell",
  "license": "MIT",
  "dependencies": {
    "classnames": "^2.2.5",
    "lodash": "^4.13.1"
  },
  "devDependencies": {
    "babel-core": "^6.9.1",
    "babel-loader": "^6.2.4",
    "babel-plugin-transform-decorators-legacy": "^1.3.4",
    "babel-plugin-transform-runtime": "^6.9.0",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-react": "^6.5.0",
    "babel-preset-stage-0": "^6.5.0"
  },
  "peerDependencies": {
    "react": ">=0.14.0"
  },
  "scripts": {
    "prebuild": "rm -rf $npm_package_config_buildDir && mkdir $npm_package_config_buildDir",
    "build": "cp -R src/* $npm_package_config_buildDir && cp package.json $npm_package_config_buildDir && cp Readme.md $npm_package_config_buildDir && cp .npmrc $npm_package_config_buildDir"
  },
  "config": {
    "buildDir": "./build"
  },
  "engines": {
    "node": ">=5.0.0",
    "npm": ">=3.0.0"
  }
}
